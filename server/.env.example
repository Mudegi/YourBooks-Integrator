# Integrator server config — copy to .env and fill in
PORT=19092
CLIENT_ORIGIN=http://localhost:19093

# Its own database (e.g. on the MariaDB instance the ERP/POS use)
DATABASE_URL="mysql://USER:PASSWORD@localhost:3308/yourbooks_integrator"
